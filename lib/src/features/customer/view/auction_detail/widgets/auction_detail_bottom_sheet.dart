import 'package:flutter/material.dart';

class AuctionDetailBottomSheet extends StatelessWidget {
  const AuctionDetailBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
